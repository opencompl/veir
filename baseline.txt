# Baseline before any Felt port changes
# Branch: llzkfelt_test1, base: main @ 0882538
# Date: 2026-05-01
# Host: 14 cores, 62GB RAM, 19GB swap
# Toolchain: Lean 4.31.0-nightly-2026-04-29 / Lake 5.0.0-src

## lake build

CLEAN (205/205 targets) on retry.

First attempt failed with exit 143 (SIGTERM) on
Veir.Rewriter.GetSet.{DetachOp,CreateOp} due to an elan toolchain-lock
race at startup (another process held nightly-2026-04-29.lock).
Not OOM, not a real Lean error. Retry was clean.

## lake test

CLEAN. UnitTest target builds (40/40); unit tests run through.

## uv run lit Test/ -v

263 of 264 pass. The only failure is the forcing-function test we just added:

  FAIL: VeIR :: Felt/identity.mlir (3 of 264)

It fails because `felt.const` is an unregistered op (parser falls back to
`builtin.unregistered`):

  | Couldn't match "felt.const"() <{"value" = 42 : i256}>...
  |   %5 = "builtin.unregistered"() : () -> !felt.type

Phase 0 complete: harness in place, forcing function failing for the right
reason, rest of suite green.

================================================================
# Baseline at tier-1-complete
# Branch: llzkfelt_test1, base: main @ 3663d6e (post-sync)
# Date: 2026-05-15
# Toolchain: Lean 4.31.0-nightly-2026-04-29 → nightly-2026-05-14 (bumped via upstream merge)

## lake build

CLEAN (255/255 targets).
Gains since the prior 213-job baseline: +34 from upstream's new modules
(HW dialect, FP/EDyadic/EScientificBV/ExtRat/State, FloatFormat, HW data
layer, Bitblast / Simp for LLVM.Int, BlockArguments rewriter pieces,
GetSetTactic) plus +8 from LLZK ports' per-dialect Properties/OpInfo
files (Felt, String, Include, Bool — RAM/Cast/Constrain have no
per-dialect files because their ops have no attributes).

## lake test

CLEAN. UnitTest target now 104 jobs (was 40; expanded by upstream
Bitblasting and FP tests).

## uv run lit Test/ -v

310 of 310 pass. Gains since 263-baseline:
  +34 from upstream's new lit tests (Interpreter poison cases,
    LLVM.getelementptr/fastntt, HW/identity, Arith divsi/divui/rem*
    edge cases, etc.)
  +13 from LLZK Tier 1: 7 identity.mlir + 6 invalid.mlir (String
    didn't have an invalid test until 2026-05-15)
  Net total: 310

## Coverage delta since baseline

LLZK dialects (harness/coverage.md §Dialects):
  felt        ❌ → ✅ (with caveats: traits, custom asm, #felt.const)
  string      ❌ → ✅ (with caveats: traits, custom asm)
  include     ❌ → ✅ (caveats: HasParent<ModuleOp>, Symbol traits)
  ram         ❌ → ✅ (caveats: MemRead/MemWrite/WitnessGen)
  cast        ❌ → ✅ (caveats: InferTypeOpInterface, type checks)
  bool        ❌ → ⚠️ Partial (cmp deferred, traits not encoded)
  constrain   ❌ → ⚠️ Partial (in deferred, ConstraintOpInterface,
                ConstraintGen, traits not encoded)

Infrastructure (harness/coverage.md §Symbols, §Types):
  FlatSymbolRefAttr  ❌ → ✅ (upstream PR #533)
  !string.type       ❌ → ✅
  !felt.type         ❌ → ✅ (with caveats: name preserved, no field semantics)
  index type         ❌ → ✅ (added inline during Phase A.4)

Verification machinery (harness/coverage.md §Verification):
  WfIRContext-based verifier  ❌ → ✅ (upstream restructure)
  Unregistered op-name preservation  ❌ → ✅ (upstream PR #569)
    NOTE: this *worsened* Gotcha 2 — typed-path negative tests
    (Test/LLZK/<dialect>/invalid.mlir) are now mandatory for catch.

Tags:
  port-string-v1, port-include-v1, tier-1-complete

================================================================
# Baseline at 83a60e4 (structured #felt<const N> attribute)
# Branch: llzkfelt_test1, base: main @ 3663d6e
# Date: 2026-05-17

## lake build

CLEAN (267/267 targets). Gains since the prior 255-baseline:
+12 from per-dialect LLZK files added during Phase D.1 Global,
verified-pass pilots E.1-E.4, and Phase D.4 Bool full.

## lake test

CLEAN. UnitTest target 110 jobs.

## uv run lit Test/ -v

327 total tests.
  Without LLZK_OPT: 319 PASS + 8 UNSUPPORTED (differentials skipped)
  With    LLZK_OPT: 322 PASS + 5 XFAIL + 0 FAIL

## scripts/check-llzk-quality-gates.sh

All gates PASS (one informational WARN about historical lit-count refs).

## Differential testing status (with LLZK_OPT set)

PASS (4): String/literals, Include/from, Felt/arith (newly un-XFAILed
  by the structured #felt<const N> attribute), and the verified-pass
  scaffolding.
XFAIL (5): Bool/logical, Cast/casts, Constrain/eq, RAM/load_store,
  Global/def_read_write — all require a function.def wrapper with
  specific attributes; lifts at Phase G.1.

## Coverage delta since 310-baseline

LLZK dialects:
  bool        ⚠️ → ✅ (cmp via IntegerAttr workaround, Phase D.4)
  global      ❌ → ✅ (Phase D.1, then post-llzk-opt sym_name fix)

Verification machinery:
  First verified LLZK pass         ❌ → ✅ (Phase E.1)
  + 3 more verified pilots         (E.2, E.3, E.4 — all in felt-combine)

Infrastructure:
  Per-dialect structured attribute  ❌ → ✅ (#felt<const N>, 2026-05-17)
  Differential testing               offline → 4 PASS + 5 XFAIL
  LLZK quality-gates script           ❌ → ✅
  llzk-quality-gates.yml CI           ❌ → ✅

Tags:
  + verif-felt-right-identity-v1, verif-felt-combine-v2


# Baseline at 9e665c69 (post-upstream-merge of 32 commits + Phase E.5/F.3-F.5)
# Branch: llzkfelt_test1
# Date: 2026-05-20
# Toolchain: leanprover/lean4:v4.30.0-rc2 (pinned for Mathlib compat;
#   upstream PR #595 nightly-2026-05-17 bump rejected)

## lake build

CLEAN (2563 build jobs). The job-count jump from 267 reflects two
things: (1) Phase E.5 (2026-05-19) pulled in Mathlib as a transitive
dep for `ZMod p`, and (2) the upstream merge added the Dataflow
Framework (#264), `VEIR_ROUNDTRIP` (#605), ParserError refactor
(#587/#588), and twenty-some smaller modules. Three pre-existing
unused-simp-arg warnings at `Veir/Verifier.lean:2107` originate
upstream (post-ParserError refactor) — not blocking.

## lake test

CLEAN. UnitTest target 122 jobs (was 110 pre-merge; +12 from upstream
PRs adding ParserError tests, DataFlowFramework tests, and
FP.EDyadic.Pack tests).

## uv run lit Test/ -v

356 total tests.
  Without LLZK_OPT: 347 PASS + 9 UNSUPPORTED (differentials skipped)
  With    LLZK_OPT: 355 PASS + 1 UNSUPPORTED + 0 XFAIL + 0 FAIL

## scripts/check-llzk-quality-gates.sh

All gates PASS.

## Differential testing status (with LLZK_OPT set)

PASS (8): String/literals, Include/from, Felt/arith, Bool/logical,
  Cast/casts, Constrain/eq, RAM/load_store, Global/def_read_write.
  Phase F.5 lifted the last 5 via function.def wrappers
  (2026-05-18). Function dialect itself has no differential yet —
  see REVIEW_2026-05-20.md §3 (added 2026-05-20).

## Coverage delta since 310-baseline (post-FeltConstAttr)

Verification machinery:
  Felt semantic model       provisional Int → ZMod p (Phase E.5)
  Verified Felt rewrites    4 → 15 (Tier 1+2 follow-up, all axiom-clean)

Phase F:
  F.1 (design note)         ❌ → ✅ (`harness/regions-design.md`)
  F.3 (terminator classifier)  ❌ → ✅ (`Veir/IR/Terminators.lean`;
                                       verify-time check still F.3.1)
  F.4 (symbol-table walker, unverified) ❌ → ✅ (api-only, no consumers)
  F.5 (function.def + function.return)  ❌ → ✅ (lifted 5 XFAILs)

Phase F status correction (2026-05-20):
  Block args as ValuePtr    coverage.md was ❌; corrected to ⚠️
                            (data structure existed all along)

Upstream merge (32 commits, committed as 9e665c69):
  Dataflow Analysis Framework (#264)         pulled in, unused by LLZK
  VariableState interpreter (#599)
  VEIR_ROUNDTRIP substitution (#605)         used by 20 upstream tests
  ParserError type (#587/#588)               required our throwString
                                              adaptation in AttrParser
  Nested-scope parser (#576)
  caret-style printer (#589)

Tags (pending):
  + merge-upstream-2026-05-20
